// import types from '../types';

const actions = {
    // getType({ commit }, data) {
    //     // 提交一个名为 increment 的变化，名字可自定义，可以认为是类型名，与下方 mutations 中的 increment 对应
    //     // commit 提交变化，修改数据的唯一方式就是显式的提交 mutations
    //     // commit(types.USER_INFO_TYPE, data);
    // },

};

export default actions;
